import Navigation from './Navigation';

const Header = () => {
    return (
        <header className="header">
            <div>Bryan Tran</div>
            <Navigation />
        </header>
    );
};

export default Header;
