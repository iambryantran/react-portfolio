import Navigation from './Navigation';

const Header = () => {
    return (
        <header>
            <div className="name">Bryan Tran</div>
            <Navigation />
        </header>
    );
};

export default Header;
